[project]
name = "m-store-l2"
version = "0.1.0"
requires-python = ">=3.11,<3.14"

dependencies = [
    "langgraph-sdk",
    "python-dotenv",
    "typing-extensions",
]

[tool.ruff]
lint.select = ["E", "F", "I", "UP"]
lint.ignore = ["T201", "E501", "UP006", "UP007"]
