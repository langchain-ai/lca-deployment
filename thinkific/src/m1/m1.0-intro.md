# What is LangSmith Deployment?

<details>
<summary>🎥 Video Walk-through</summary>
<br>

<Video src="https://share.descript.com/view/cPFktdYhdYI" />

</details>

LangSmith Deployment is a workflow orchestration runtime purpose-built for agent workloads. It provides the managed infrastructure agents need to run reliably in production at scale, supporting the full lifecycle from local development to deployment.

Examining the components of a deployment can clarify what this means in practice. The diagram below takes you through the steps and packages that combine to build a deployment.

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="sd-wrap" id="ss-deploy"></div>

<script>
buildSlideshow({
  id: 'ss-deploy',
  slides: [
    { src: 'images/deploy-step1.png', tag: 'Slide 1 of 8', caption: `To deploy, you start with the agent (or agents) that you have been refining locally. These are defined as a graph.` },
    { src: 'images/deploy-step2.png', tag: 'Slide 2 of 8', caption: `A Graph is compiled and run with the LangGraph runtime. A deployment will package the runtime with your graph.` },
    { src: 'images/deploy-step3.png', tag: 'Slide 3 of 8', caption: `To execute the nodes you will need the interpreter for Python/TypeScript, the packages you use, and an operating system to run these on. You accomplish this by creating a docker container.` },
    { src: 'images/deploy-step4.png', tag: 'Slide 4 of 8', caption: `An API service terminates HTTP requests while a queue manages them. An added worker process will manage the interaction with the queue and support multiple requests (where m is the number of simultaneous runs per container).` },
    { src: 'images/deploy-step5.png', tag: 'Slide 5 of 8', caption: `A deployment can support multiple containers. Containers are spun up and down, runs can migrate between active containers.` },
    { src: 'images/deploy-step6.png', tag: 'Slide 6 of 8', caption: `Run or Thread state is saved in a persistent database, the checkpointer. As runs start, pause, or migrate, their state is held here.` },
    { src: 'images/deploy-step7.png', tag: 'Slide 7 of 8', caption: `Similarly, nodes may need to store data and have it persist across containers and runs. This is provided by the store.` },
    { src: 'images/deploy-step8.png', tag: 'Slide 8 of 8', caption: `The deployment also persists the records that describe how runs are parameterized and what's happened — assistants (config and context), threads (conversation metadata), and runs (execution history).` },
  ]
});
</script>


In the following lessons, you will deploy agents and dig into the details, implications, and benefits of this organization. 


# Course Format
The course is broken into modules. Modules focus on aspects of deployment. The modules will generally follow three steps:

1) Introduce a concept
2) Use the concept in simple examples
3) Use the concept in a larger example: A tutor agent.

The course text can generally be used stand-alone. The video will summarize the content, run examples, and work in the UI. The UI content will generally not be in the text.

LangSmith Deployments is evolving along with agents - rapidly! The course may differ from the UI over time, and new modules may be added as new features are added. 